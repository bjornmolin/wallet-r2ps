# SPDX-FileCopyrightText: 2025 diggsweden/wallet-r2ps
#
# SPDX-License-Identifier: CC0-1.0

name: OpenSSF Scorecard Analysis

on:
  push:
    branches:
      - main
  schedule:
    # Saturdays at 02:40 UTC
    - cron: "40 2 * * 6"
    # Wednesdays at 02:40 UTC
    - cron: "40 2 * * 3"
  workflow_dispatch:

permissions:
  contents: read # Best Security practice. Jobs only get read as base, and then permissions are added as needed

jobs:
  scorecard-analysis:
    permissions:
      contents: read
      security-events: write
      id-token: write
    # SMOKE TEST: pinned to bjornmolin/reusable-ci feat/rust-first-class-support.
    # Switch to diggsweden/reusable-ci@<v2.8.0-sha> once the official tag is cut.
    uses: bjornmolin/reusable-ci/.github/workflows/security-openssf-scorecard.yml@733cecf5f599210a7ef80bad6fb5c8421430b4b4 # feat/rust-first-class-support
    with:
      publish-results: true
